# Python Native HTTP

Standalone API From Hell implementation using Python's standard-library HTTP server.

Run from this folder:

```bash
python server.py
```

Configuration:

- `PORT`, default `3001`
- `FROMHELL_PREFIX`, default `/fromhell`
- `FROMHELL_CATALOG`, default `../../catalog/fromhell-catalog.json`
